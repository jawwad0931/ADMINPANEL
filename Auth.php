<?php
// session_start();

// Check if the user is already logged in
// if (isset($_SESSION['auth'])) {
//     // User is already logged in, redirect to the dashboard or another page
//     header('location: home.php');
//     exit(0);
// }

// Check if the login form is submitted
// if ($_SERVER['REQUEST_METHOD'] === 'POST') {
//     // Process the login form and validate credentials
//     // If login is successful, set the auth session variable
//     $_SESSION['auth'] = true;

//     // Redirect to the dashboard or another page
//     header('location: home.php');
//     exit(0);
// } else {
    // Login failed, display an error message or handle it accordingly
    // $error_message = "Invalid username or password";
// }
?>

<!-- HTML login form goes here -->
